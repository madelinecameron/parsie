const debug = require('debug')('parsie')

module.exports = {
  parse: (input) => {
    debug(`Parsing is hard and every library is going to get it wrong. \nEmbrace the chaos and trust your users know their info.`)

    return input
  }
}